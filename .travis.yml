language: python
python:
        - "3.5"
        - "3.6"
        - "3.7"
        - "3.8"
install:
        - pip install nose
        - pip install -e .
        # python 3.2 apparently doesn't have mock
        - pip install mock
script: python setup.py test
